# Since .env is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.

# Flask configuration cryptographic key
# This can be generated using the follwowing command:
# python3 -c "import uuid; print(uuid.uuid4().hex)"
SECRET_KEY=""

# Twitch OAuth
TWITCH_CLIENT_ID=""
TWITCH_CLIENT_SECRET=""

# Twitch OAuth redirect URI
# This should match the redirect URI set in your Twitch application settings
TWITCH_OAUTH_REDIRECT_URI="http://localhost:5000"

# Twitch requires an embed parent to be set for embedding content
# Set to "localhost" for local development
EMBED_PARENT="localhost"

# Twitch broadcaster ID to automatically fetch clips for
BROADCASTER_ID=""

# Twitch game ID to automatically fetch clips for
# This is mutually exclusive with BROADCASTER_ID, if both are provided, only BROADCASTER_ID is used
GAME_ID=""

# Default superadmin users, used when seeding the database
SUPERADMIN_NAMES=

# Database URL for SQLAlchemy
# Format: mysql+pymysql://<username>:<password>@<host>:<port>/<database>
# Example: mysql+pymysql://user123:password123@localhost:3306/cliprepo
DATABASE_URL=

# Filename to be used as the site logo (upper left corner)
# Available files can be viewed under cliprepo/app/static
# If this is omitted, a default logo will be used
LOGO_FILENAME=""

# The date from which to start fetching clips
CLIPS_START_DATE=""